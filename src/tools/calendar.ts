// Calendar tools registration — implementation pending Phase 4
import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

export function registerCalendarTools(_server: McpServer): void {
  // apple_cal_list, apple_cal_events, apple_cal_get_event,
  // apple_cal_create, apple_cal_update, apple_cal_delete
}
