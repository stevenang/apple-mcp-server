// Reminders tools registration — implementation pending Phase 4
import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

export function registerRemindersTools(_server: McpServer): void {
  // apple_reminders_lists, apple_reminders_items, apple_reminders_create,
  // apple_reminders_complete, apple_reminders_delete
}
