// Mail tools registration — implementation pending Phase 4
import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

export function registerMailTools(_server: McpServer): void {
  // apple_mail_list, apple_mail_search, apple_mail_read,
  // apple_mail_folders, apple_mail_send, apple_mail_move
}
