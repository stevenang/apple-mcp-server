// Contacts tools registration — implementation pending Phase 4
import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

export function registerContactsTools(_server: McpServer): void {
  // apple_contacts_list, apple_contacts_search, apple_contacts_get,
  // apple_contacts_create, apple_contacts_update
}
